# التحقق من إعداد Gmail API
# Verify Gmail API Setup

## ✅ المتغيرات المطلوبة (يجب أن تكون موجودة):

1. ✅ `GMAIL_CLIENT_ID`
2. ✅ `GMAIL_CLIENT_SECRET`
3. ✅ `GMAIL_REFRESH_TOKEN`
4. ✅ `GMAIL_USER_EMAIL` = `info@baytaljazeera.com`
5. ✅ `GMAIL_FROM_NAME` = `بيت الجزيرة`

---

## 🔍 الخطوة 1: التحقق من Logs في Render

1. اذهب إلى Render Dashboard
2. اختر Service: **baytaljazeera-backend**
3. اضغط على **Logs** في القائمة الجانبية
4. ابحث عن هذه الرسالة:

### ✅ إذا رأيت:
```
✅ Gmail API initialized successfully
```

**ممتاز!** النظام جاهز ويعمل. ✅

### ❌ إذا رأيت:
```
⚠️ Gmail credentials not set. Email sending will be disabled.
```

**المشكلة:** أحد المتغيرات مفقود أو غير صحيح.

**الحل:**
- تأكد من وجود جميع المتغيرات الخمسة
- تأكد من أن القيم صحيحة (خاصة Refresh Token)
- أعد تشغيل الخادم يدوياً من Render Dashboard

---

## 🧪 الخطوة 2: اختبار النظام

### اختبار 1: تسجيل مستخدم جديد

1. اذهب إلى موقعك: `https://baytaljazeera.com/register`
2. سجل مستخدم جديد بإيميل حقيقي
3. بعد التسجيل، تحقق من صندوق الوارد
4. يجب أن تصلك رسالة تأكيد الإيميل

### اختبار 2: تأكيد الإيميل

1. اضغط على رابط التأكيد في الإيميل
2. يجب أن يتم تأكيد الإيميل بنجاح
3. بعد التأكيد، يجب أن تصلك رسالة ترحيب

### اختبار 3: إعادة تعيين كلمة المرور

1. اذهب إلى `/forgot-password`
2. أدخل إيميل مسجل
3. تحقق من صندوق الوارد
4. يجب أن تصلك رسالة إعادة تعيين كلمة المرور

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا تصل الإيميلات

**التحقق:**
1. تحقق من Logs في Render - هل ترى أخطاء؟
2. تحقق من صندوق Spam/Junk
3. تأكد من أن Refresh Token صحيح

**الحل:**
- راجع الخطوات في `GMAIL_SETUP_STEP_BY_STEP.md`
- احصل على Refresh Token جديد إذا لزم الأمر

### المشكلة: "invalid_grant" في Logs

**السبب:** Refresh Token غير صحيح أو منتهي

**الحل:**
1. احصل على Refresh Token جديد من OAuth 2.0 Playground
2. حدّث `GMAIL_REFRESH_TOKEN` في Render Dashboard
3. أعد تشغيل الخادم

### المشكلة: "unauthorized_client"

**السبب:** Client ID أو Client Secret غير صحيح

**الحل:**
1. راجع Google Cloud Console → Credentials
2. تأكد من نسخ Client ID و Client Secret بشكل صحيح
3. حدّث المتغيرات في Render Dashboard

---

## 📧 أنواع الإيميلات المدعومة

- ✅ إيميل تأكيد الإيميل (عند التسجيل)
- ✅ إيميل الترحيب (بعد تأكيد الإيميل)
- ✅ إعادة تعيين كلمة المرور
- ✅ إعادة إرسال رابط التأكيد

---

## ✅ قائمة التحقق النهائية

- [ ] جميع المتغيرات الخمسة موجودة في Render
- [ ] Logs تظهر "✅ Gmail API initialized successfully"
- [ ] تم اختبار تسجيل مستخدم جديد
- [ ] وصل إيميل التأكيد
- [ ] تم تأكيد الإيميل بنجاح
- [ ] وصل إيميل الترحيب بعد التأكيد

---

**آخر تحديث:** 2025-01-24
