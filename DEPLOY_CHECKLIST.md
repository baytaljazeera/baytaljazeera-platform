# ✅ قائمة النشر — تأكد أن التعديلات تصل لـ Vercel/Render

## المشكلة
Vercel و Render ينشران من **GitHub**. إذا لم ترفع التعديلات إلى GitHub، لن تظهر على الموقع.

## الحل — بعد كل تعديل

```bash
# 1. تحقق من التعديلات
git status

# 2. أضف الملفات المعدلة
git add .

# 3. احفظ التعديلات
git commit -m "وصف التعديل"

# 4. ارفع إلى GitHub (هذا يفعّل النشر التلقائي)
git push origin main
```

## اختصار سريع (سطر واحد)

```bash
git add . && git commit -m "تحديث" && git push origin main
```

## ملاحظة
- **Vercel** ينشر الفرونتند تلقائياً بعد كل `git push`
- **Render** ينشر الباكند تلقائياً بعد كل `git push`
- انتظر 2–5 دقائق بعد الـ push حتى يكتمل النشر
