# خطوات حل مشكلة "العملية غير موجودة" وعدم استجابة الخادم — شرح واضح خطوة بخطوة

---

## أولاً: فهم المشكلة ببساطة

- **ما يحدث:** لما تضغط "توليد الفيديو" المتصفح يرسل طلباً لسيرفرك الموجود على موقع اسمه **Render**.
- **المشكلة:** أحياناً السيرفر "نائم" (بعد 15 دقيقة بدون زيارات ينام في الخطة المجانية)، أو الرابط لا يصل. فالمتصفح يظن أن "العملية غير موجودة" بينما السبب الحقيقي: **السيرفر غير متاح أو لم يرد بعد**.

---

## ثانياً: ماذا تم إصلاحه في الكود (لا تحتاج تفعل شيء له)

- تم تعديل الكود بحيث:
  - إذا كان الخطأ من **الشبكة** (السيرفر لا يرد) تظهر رسالة: **"عذراً، الخادم لا يستجيب حالياً. قد يكون قيد التشغيل..."** بدلاً من "العملية غير موجودة".
  - يتم الانتظار قليلاً ثم إعادة المحاولة تلقائياً (انتظار يتزايد) قبل الاستسلام.
- هذا يقلل الالتباس ويعطي السيرفر وقتاً ليعمل مرة ثانية.

---

## ثالثاً: ماذا **أنت** تفعل — خطوات واضحة

### الخطوة 1: التأكد أن مشروعك على Render يعمل

1. افتح المتصفح وادخل إلى موقع: **https://dashboard.render.com**
2. سجّل دخولك.
3. من القائمة اختر **"Web Services"** (أو الخدمات اللي أنشأتها).
4. ابحث عن خدمة الـ **Backend** (مثلاً اسمها "baytaljazeera-backend" أو شبيه).
5. شوف **الحالة** بجانب الخدمة:
   - إذا كانت **"Live"** أو **"Running"** → السيرفر مفترض يعمل. (قد يكون نائماً لكنه موجود.)
   - إذا كانت **"Suspended"** أو **"Failed"** أو **"Crashed"** → هنا المشكلة من Render نفسه؛ يجب إما تجديد الخطة أو إعادة تشغيل الخدمة من لوحة Render.

**الخلاصة:** تأكد أن الخدمة ليست معلقة ولا متوقفة. إذا معلقة، فعّلها أو أعد تشغيلها من Render.

---

### الخطوة 2: "إيقاظ" السيرفر قبل تجربة الفيديو (مهم جداً)

على الخطة المجانية، السيرفر ينام بعد فترة بدون زيارات. أول طلب بعد النوم يأخذ وقتاً (حتى دقيقة أحياناً) حتى "يستيقظ".

**ما تفعله:**

1. افتح **تبويب جديد** في المتصفح.
2. في شريط العنوان اكتب أو الصق هذا الرابط (بدون تغيير):
   ```
   https://baytaljazeera-backend.onrender.com/api/health
   ```
3. اضغط **Enter**.
4. **انتظر حتى تفتح الصفحة** (قد تأخذ 30 ثانية إلى دقيقة).
5. إذا ظهر شيء مثل: `{"status":"healthy",...}` أو نص JSON → معناها السيرفر استيقظ ويعمل.
6. إذا ظهر خطأ "لا يمكن الوصول للموقع" أو "الموقع غير متاح" → انتظر دقيقة وحاول مرة ثانية، أو راجع الخطوة 1 (حالة الخدمة على Render).

**الخلاصة:** قبل ما تجرب "توليد الفيديو"، افتح رابط الـ health مرة واحدة وانتظر حتى يرد. بعدها جرّب التوليد من الموقع.

---

### الخطوة 3: تجربة توليد الفيديو من الموقع

1. ادخل إلى موقعك (صفحة إضافة إعلان جديد أو المكان اللي فيه زر "توليد الفيديو").
2. املأ البيانات واختر الصور كما تفعل عادة.
3. اضغط زر **"توليد الفيديو"** (أو الاسم اللي عندك).
4. **لا تغلق الصفحة.** انتظر:
   - إذا ظهرت رسالة **"جاري التوليد..."** أو شريط تقدم → معناها الطلب وصل. استمر بالانتظار (قد يأخذ عدة دقائق).
   - إذا ظهرت **"عذراً، الخادم لا يستجيب حالياً. قد يكون قيد التشغيل..."** → معناها السيرفر لم يرد بعد. انتظر **دقيقة كاملة** ثم نفّذ **الخطوة 2** مرة ثانية (افتح رابط الـ health وانتظر حتى يرد)، بعدها جرّب زر "توليد الفيديو" مرة ثانية.
   - إذا ظهرت **"عملية التوليد غير موجودة أو انتهت صلاحيتها"** بعد وقت طويل جداً (مثلاً 10 دقائق) → غالباً السيرفر أعاد تشغيل نفسه وفقد حالة العملية. هنا الحل الأفضل هو تفعيل Redis (الخطوة 5).

**الخلاصة:** بعد إيقاظ السيرفر (الخطوة 2)، جرّب التوليد وانتظر. إذا ظهرت رسالة "الخادم لا يستجيب" فانتظر دقيقة وأعد فتح الـ health ثم جرّب مرة ثانية.

---

### الخطوة 4: إذا استمرت المشكلة — ماذا تراجع؟

- **رابط الـ Backend في الموقع:** تأكد أن الموقع (الفرونتند) يستخدم نفس الرابط اللي تستخدمه في المتصفح، أي:
  `https://baytaljazeera-backend.onrender.com`
  (أو الرابط الصحيح اللي في إعداداتك). إذا كان الرابط غلط، الطلبات لن تصل.
- **الإنترنت:** جرّب من شبكة ثانية أو من جوالك (واي فاي أو بيانات) للتأكد أن المشكلة ليست من الشبكة عندك.

**الخلاصة:** تأكد من صحة رابط الـ Backend ومن أن النت يعمل.

---

### الخطوة 5 (اختيارية): تخزين حالة الفيديو حتى بعد إعادة تشغيل السيرفر — Redis

- **لماذا:** إذا السيرفر "استعاد تشغيله" (Restart) وهو يخزّن حالة العمليات في الذاكرة فقط، قد تفقد العملية وتظهر "العملية غير موجودة" حتى لو كانت قد بدأت.
- **الحل المعماري:** استخدام **Redis** لحفظ حالة عمليات الفيديو. عندها حتى لو السيرفر أعاد التشغيل، يجد الحالة في Redis ولا تضيع.

**ما تفعله إن أردت:**

1. أنشئ حساباً مجانياً على **Upstash** (مزود Redis): **https://upstash.com**
2. أنشئ قاعدة Redis جديدة واحصل على **Redis URL** (شكلها تقريباً: `rediss://default:كلمة_سر@عنوان:6379`).
3. في **Render** → خدمة الـ Backend → **Environment** (المتغيرات).
4. أضف متغيراً جديداً:
   - الاسم: `REDIS_URL`
   - القيمة: الرابط اللي نسخته من Upstash.
5. احفظ ثم أعد نشر الخدمة (Deploy) من Render.

بعد ذلك الكود (إن كان مضبوطاً لاستخدام Redis للـ cache) سيحفظ حالة عمليات الفيديو في Redis ولن تضيع عند إعادة تشغيل السيرفر.

**الخلاصة:** Redis يحل مشكلة "العملية غير موجودة" عندما تكون بسبب إعادة تشغيل السيرفر وليس بسبب أن السيرفر نائم.

---

## ملخص سريع — ترتيب ما تفعله

| الرقم | ماذا تفعل |
|-------|-----------|
| 1 | تدخل Render وتتأكد أن خدمة الـ Backend **Live** وليست Suspended/Crashed. |
| 2 | تفتح في المتصفح: `https://baytaljazeera-backend.onrender.com/api/health` وتنتظر حتى تفتح الصفحة (إيقاظ السيرفر). |
| 3 | ترجع لموقعك وتضغط "توليد الفيديو" وتنتظر. إذا ظهرت "الخادم لا يستجيب" تنتظر دقيقة وتكرر الخطوة 2 ثم 3. |
| 4 | تتأكد من صحة رابط الـ Backend في الموقع ومن الإنترنت. |
| 5 | (اختياري) تضيف Redis في Render لحفظ حالة الفيديو حتى بعد إعادة التشغيل. |

---

## جملة واحدة تختصر كل شيء

**افتح رابط الـ health في المتصفح وانتظر حتى يرد، ثم جرّب "توليد الفيديو" من الموقع وانتظر؛ إذا ظهرت "الخادم لا يستجيب" انتظر دقيقة وأعد فتح الـ health ثم جرّب مرة ثانية.**

إذا نفذت الخطوات وما زال السلوك نفسه، اذكر بالضبط: أي رسالة تظهر، ومتى (فوراً أم بعد كم دقيقة)، وسنضبط الخطوات على حالتك.
