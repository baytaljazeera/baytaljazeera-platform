# 🎬 دليل ضغط الفيديو للمنصة

## المشكلة
المنصة تقبل فيديوهات حتى **10 MB** فقط، والفيديو الحالي **11.4 MB**.

## الحل: ضغط الفيديو

### الطريقة 1: استخدام FFmpeg (الأفضل)

```bash
# ضغط الفيديو إلى أقل من 10 MB مع الحفاظ على الجودة
ffmpeg -i "دعاية الدكتوره سعاد نواصري.mp4" \
  -c:v libx264 \
  -crf 28 \
  -preset fast \
  -c:a aac \
  -b:a 128k \
  -movflags +faststart \
  "دعاية_مضغوط.mp4"
```

**الشرح:**
- `-crf 28`: جودة متوسطة (أقل = جودة أعلى، أكبر = ملف أصغر)
- `-preset fast`: ضغط سريع
- `-b:a 128k`: جودة صوت جيدة

### الطريقة 2: تقليل الدقة (إذا كان الضغط غير كافي)

```bash
# تقليل الدقة إلى 1280x720 مع ضغط
ffmpeg -i "دعاية الدكتوره سعاد نواصري.mp4" \
  -vf "scale=1280:720" \
  -c:v libx264 \
  -crf 28 \
  -preset fast \
  -c:a aac \
  -b:a 128k \
  -movflags +faststart \
  "دعاية_مضغوط_720p.mp4"
```

### الطريقة 3: تقليل معدل الإطارات (إذا لزم الأمر)

```bash
# تقليل إلى 24 fps مع ضغط
ffmpeg -i "دعاية الدكتوره سعاد نواصري.mp4" \
  -r 24 \
  -c:v libx264 \
  -crf 28 \
  -preset fast \
  -c:a aac \
  -b:a 128k \
  -movflags +faststart \
  "دعاية_مضغوط_24fps.mp4"
```

## التحقق من الحجم

بعد الضغط، تحقق من الحجم:

```bash
# على macOS
ls -lh "دعاية_مضغوط.mp4"

# يجب أن يكون أقل من 10 MB
```

## نصائح إضافية

1. **جرب CRF مختلفة:**
   - `-crf 28`: ملف أصغر (جودة متوسطة)
   - `-crf 23`: توازن جيد (جودة عالية)
   - `-crf 18`: جودة عالية جداً (ملف أكبر)

2. **إذا كان الفيديو لا يزال كبيراً:**
   - قلل الدقة إلى 1280x720
   - قلل معدل الإطارات إلى 24 fps
   - قلل جودة الصوت إلى 96k

3. **للحصول على أفضل توازن:**
   ```bash
   ffmpeg -i "input.mp4" \
     -vf "scale=1280:720" \
     -r 24 \
     -c:v libx264 \
     -crf 28 \
     -preset fast \
     -c:a aac \
     -b:a 96k \
     -movflags +faststart \
     "output.mp4"
   ```

## أدوات بديلة

### 1. HandBrake (واجهة رسومية)
- تحميل: https://handbrake.fr/
- سهل الاستخدام
- يدعم ضغط MP4

### 2. Online Tools
- CloudConvert: https://cloudconvert.com/
- FreeConvert: https://www.freeconvert.com/

## ملاحظات مهمة

✅ **المسموح:**
- MP4 (H.264 + AAC)
- حتى 10 MB
- حتى 75 ثانية (باقة رجال الأعمال)
- أي دقة

❌ **غير المسموح:**
- ملفات أكبر من 10 MB
- صيغ أخرى غير MP4/WebM
- فيديوهات أطول من 75 ثانية

---

**بعد الضغط، يمكنك رفع الفيديو على المنصة!** 🎉
